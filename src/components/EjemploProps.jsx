 const EjemploProps = ({wellcome}) => {
    console.log(wellcome);
    //si no le pongo el export, no lo voy a poder usar en otro lado, hay distintos tipos de export
    //si va a recibir una prop le tengo que escribir algo entre paraentesis... para que el sepa que viene...
  return (
    <div>ejemploProps</div>

    //Un componente retorna un solo elemento, siempre retorna codigo JSX
  )
}

export default EjemploProps